# Render Blueprint for TechnoPath SEAIT Guide Map - v6
# Deploys: Frontend (Static Site) + Backend (Web Service) + SQLite Database

services:
  # Frontend - Vue.js Static Site
  - type: web
    name: technopath-frontend
    runtime: node
    plan: free
    buildCommand: cd frontend && npm install && npm run build
    startCommand: cd frontend && npx serve -s dist -l $PORT
    envVars:
      - key: NODE_VERSION
        value: "18.20.4"
      - key: VITE_API_BASE_URL
        value: https://technopath-backend-or73.onrender.com/api

  # Backend - Django Web Service
  - type: web
    name: technopath-backend
    runtime: python
    plan: free
    rootDir: backend_django
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: bash render_start.sh
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: "localhost,127.0.0.1,.onrender.com,technopath-backend.onrender.com,technopath-backend-or73.onrender.com"
      - key: CORS_ALLOWED_ORIGINS
        value: "https://technopath-frontend-or73.onrender.com,https://technopath-frontend.onrender.com,http://localhost:5173"
      # DATABASE_URL must be set manually in the Render dashboard.
      # Create a PostgreSQL database in Render and paste the Internal Database URL.
      # DO NOT use sqlite:///tmp/ — /tmp is wiped on every restart (all data lost).
      # - key: DATABASE_URL
      #   value: (set in Render dashboard, never commit this)
